type Eit_types::Jenkins::Plugin = Struct[{
  version       => Optional[Eit_types::Version],
  manage_config => Optional[Boolean],
  enabled       => Optional[Boolean],
  pin           => Optional[Boolean],
}]
