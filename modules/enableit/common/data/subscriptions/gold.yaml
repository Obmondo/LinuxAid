---
common::system::classes:
  - common::system::updates

common::default::classes:
  - common::setup::obmondo_admin
  - common::system::authentication::sudo

common::system::updates::enable: true
common::system::updates::manage: true

common::setup::obmondo_admin::manage: true
common::setup::obmondo_admin::ensure: true
