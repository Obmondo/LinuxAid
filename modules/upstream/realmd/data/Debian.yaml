---
realmd::krb_client_package_name: krb5-user
realmd::required_packages:
  libnss-sss:
    ensure: installed
  libpam-modules:
    ensure: installed
  libpam-sss:
    ensure: installed
  packagekit:
    ensure: installed
  samba-common-bin:
    ensure: installed
  sssd-tools:
    ensure: installed
